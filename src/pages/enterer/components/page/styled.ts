import styled from "styled-components";

export const EntererWrapper = styled.div`
    max-width: 500px;
    width: 100%;
    margin: 0 auto;
    min-height: 100vh;
    padding: 7px;
    box-sizing: border-box;
    background-color: var(--bg);
    color: var(--main);
`